/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains:["localhost"]

  },
};

export default nextConfig;
    // remotePatterns: [
    //   {
    //     protocol: "http",
    //     hostname: "localhost",
    //     port: "**",
    //     pathname: "**",
    //   },
    // ],